<view>
  <view wx:for="{{place}}" wx:key="key">
    <navigator url='./detail/detail?place={{item}}' hover-class='none'>
      <view class="card">
        <view class="title">{{item}}</view>
      </view>
    </navigator>
  </view>
</view>